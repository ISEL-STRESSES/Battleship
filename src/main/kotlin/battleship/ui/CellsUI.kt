package battleship.ui

/*
import battleship.model.board.CellContent
import battleship.model.board.CellContent.*

fun CellContent.toChar(): Char {
        return when (this) {
            WATER -> ' '
            SHIP -> '#'
            HIT -> '*'
            SUNK -> 'X'
            MISS -> 'O'
            BORDER -> 'B' // hide this shit
        }
    }

 */
